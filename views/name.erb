<div class='name <%= cycle('even', 'odd') %>'>
  <h3><a href="/names/<%= name.name.downcase %>"><%= name.name %></a></h3>

	<div class='voting'>
    <span class="inline-rating">
      <ul class="star-rating" id="vote_name_<%= name.id %>">
        <li class="current-rating" style="width:<%= name.percentage_vote %>%;"><%= name.average_vote ? "#{name.average_vote}/5 stars" : "No ratings" %>.</li>
        <li><a href="#" title="1 star out of 5" class="one-star">1</a></li>
        <li><a href="#" title="2 stars out of 5" class="two-stars">2</a></li>
        <li><a href="#" title="3 stars out of 5" class="three-stars">3</a></li>
        <li><a href="#" title="4 stars out of 5" class="four-stars">4</a></li>
        <li><a href="#" title="5 stars out of 5" class="five-stars">5</a></li>
      </ul>
    </span>
	</div>

  <a href='/lists/<%= @list.url %>/<%= name.name.downcase %>/comments'>
    <%= name.comments.size > 0 ? "#{name.comments.size} comments" : "comment" %>
  </a>

	<% if logged_in? && current_user.lists.include?(@list) %>
    <div class='admin'>
      <form action="/lists/<%= @list.url %>/names/<%= name.id %>" method='post'>
        <input type='hidden' name="_method" value="delete">
        <input type='submit' value='[delete name]'>
      </form>
    </div>
  <% else %>
    <br/>
    <br/>
  <% end %>
</div>
<% reset_cycle %>
